unit QuadroPesquisa1;

interface

uses
  Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, Grids, DBGrids, DB, IBCustomDataSet, IBTable, IBDatabase,
  StdCtrls;

type
  TQuadroPesquisa = class(TForm)
    DBGrid1: TDBGrid;
    IBDatabase1: TIBDatabase;
    IBTransaction1: TIBTransaction;
    IBTable1: TIBTable;
    DataSource1: TDataSource;
    EditNome: TEdit;
    Label1: TLabel;
    Label2: TLabel;
    cbCodigo: TComboBox;
    CaixaBusca: TEdit;
    procedure CaixaBuscaChange(Sender: TObject);
    procedure FormCreate(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  QuadroPesquisa: TQuadroPesquisa;

implementation

{$R *.dfm}

procedure TQuadroPesquisa.CaixaBuscaChange(Sender: TObject);
begin
        case cbCodigo.itemIndex of
        0: begin


           end;


        1: begin

           end;
end;

end;

end.
